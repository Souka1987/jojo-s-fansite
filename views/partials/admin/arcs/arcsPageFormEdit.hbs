<section>
    <h1 class="text-center text-dark">Edition Arcs</h1>
    {{!--Ajout de l'url et la méthode --}}
    <form action="/admin/editArcs/{{ _id }}" method="POST" enctype="multipart/form-data">

        {{!-- Input Titre --}}
        <div class="form-group">
            <label for="text">Titre</label>
            <p id='ckeditor' hidden> {{ title }} </p>
            <input type="text" class="form-control" id="exampleInput" name="title" value="{{ title }}">
        </div>

        {{!-- Input Contenu --}}
        <div class="form-group">
            <label for="exampleInputTextarea">Contenu</label>
            <p id='ckeditor' hidden> {{ content }} </p>
            <textarea class="form-control" id="editor4" placeholder="example textarea" name="content" value="{{ content }}" ></textarea>
            {{!-- Rajout de l'éditeur de texte "CKeditor" --}}
             <script>
                const ed  = CKEDITOR.replace('editor4', {
                    entities: false, // Pour la suppression des caractères spéciaux
                    //uiColor: '#00000' // Coloriser les bordures de l'éditeur
                });

                ed.on('change', function (e) {// Modification du contenu de l'éditeur
                    console.log(e.editor.getData())
                });

                // Injecter les data
                const data{{ _id }} = document.getElementById('ckeditor').innerText
                CKEDITOR.instances.editor.setData(data{{ _id }})
            </script>
        </div>

        {{!-- Input Images --}}
        <div class="form-group">
            <label for="exampleFormControlFile1">Envoyer une image au format jpeg</label>
            <input type="file" class="form-control-file" id="exampleFormControlFile1" name="image" value="{{ image }}">
        </div>

        {{!-- Bouton valider --}}
        <button type="submit" class="btn btn-info">Modifier</button>
    </form>
</section>